{{> heading/navbar}}

{{> heading/title}}

{{#each articles ~}}
  <div class="card">
  <div class="card-header">
    <a href={{link}}>
    {{title}}
    </a>
  </div>
  <div class="card-body">
    <p class="card-text">Description placeholder</p>
    <a href="/delete/{{_id}}" class="btn btn-primary">Delete From Saved</a>
    <a href="/articleNotes/{{_id}}" class="btn btn-success">Article Notes</a>
  </div>
</div>
{{~/each}}

